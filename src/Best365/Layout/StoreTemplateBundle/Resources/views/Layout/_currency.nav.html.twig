<li class="dropdown">
	<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
		货币选择
		<span class="caret"></span>
	</a>
	<ul class="dropdown-menu">
		{% set index = 1 %}
		{% for currency in currencies %}
			{% if index > 1%}
			<li role="seperator" class="divider"></li> 
			{% endif %}
	        <li>
	            <a href="{{ url("best365_store_currency_switch", {'iso': currency.iso}) }}">
	            	{% if currency.iso == 'CNY' %}
	                <img src="{{ asset('assets/img/china.png') }}" />
	                <span class="margin-left-10">人民币</span>
	                {% else %}
	                <img src="{{ asset('assets/img/new_zealand.png') }}" />
	                <span class="margin-left-10">NZD</span>
	                {% endif %}
	                <!-- <img src="{{ asset('assets/img/' ~ currency.iso ~ '.png') }}" /> -->	            
	            </a>
	        </li>
	        {% set index = index + 1 %}
	    {% endfor %}
	</ul>
</li>